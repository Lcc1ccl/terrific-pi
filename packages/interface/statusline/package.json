{
  "name": "statusline",
  "version": "1.4.0",
  "description": "Configurable statusline footer for pi (HUD layout, icon modes, native OAuth quota, tokens, git, run state)",
  "type": "module",
  "private": true,
  "keywords": ["pi-package", "pi-extension", "statusline"],
  "license": "MIT",
  "pi": {
    "extensions": ["./extensions/statusline.ts"]
  },
  "scripts": {
    "test": "node --test --experimental-strip-types tests/*.test.ts",
    "typecheck": "tsc -p tsconfig.json",
    "check": "npm run typecheck && npm test"
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-ai": "0.83.0",
    "@earendil-works/pi-coding-agent": "0.83.0",
    "@earendil-works/pi-tui": "0.83.0",
    "@types/node": "^26.0.0",
    "typescript": "^6.0.3"
  },
  "files": [
    "extensions",
    "lib",
    "LICENSES",
    "package.json",
    "README.md",
    "tsconfig.json"
  ]
}
