{
  "name": "appearance",
  "version": "0.1.0",
  "description": "Pi TUI header, rounded editor, and appearance settings.",
  "type": "module",
  "private": true,
  "keywords": ["pi-package", "pi-extension", "appearance", "tui"],
  "license": "MIT",
  "pi": {
    "extensions": ["./extensions/appearance.ts"]
  },
  "scripts": {
    "test": "node --test --experimental-strip-types tests/*.test.ts",
    "typecheck": "tsc -p tsconfig.json",
    "check": "npm run typecheck && npm test"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "0.83.0",
    "@earendil-works/pi-tui": "0.83.0",
    "@types/node": "^26.0.0",
    "typescript": "^6.0.3"
  },
  "files": ["extensions", "lib", "LICENSES", "package.json", "README.md", "tsconfig.json"]
}
