{
  "name": "pilot",
  "version": "0.1.0",
  "description": "Pilot control-plane activation and Auxiliary routing spike for pi",
  "type": "module",
  "keywords": ["pi-package", "pi-extension", "pilot", "workflow"],
  "license": "MIT",
  "terrificPi": { "install": false },
  "pi": {
    "extensions": ["./extensions/pilot.ts"]
  },
  "scripts": {
    "test": "node --test --experimental-strip-types tests/*.test.ts",
    "typecheck": "tsc -p tsconfig.json",
    "check": "npm run typecheck && npm test"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "0.80.10",
    "@types/node": "^26.0.0",
    "typescript": "^6.0.3"
  },
  "files": ["extensions", "lib", "package.json", "README.md", "tsconfig.json"]
}
