{
  "name": "statusline",
  "version": "1.2.0",
  "description": "Configurable statusline footer for pi (tokens, cache, cost, context bar, git, run state)",
  "type": "module",
  "keywords": ["pi-package", "pi-extension", "statusline"],
  "license": "MIT",
  "pi": {
    "extensions": ["./extensions/statusline.ts"]
  },
  "scripts": {
    "test": "node --test --experimental-strip-types tests/*.test.ts"
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*"
  },
  "files": [
    "extensions",
    "lib",
    "package.json",
    "README.md"
  ]
}
