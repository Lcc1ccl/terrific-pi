{
  "name": "fast",
  "version": "1.0.1",
  "description": "Toggle OpenAI Priority processing via /fast (service_tier=priority)",
  "type": "module",
  "keywords": ["pi-package", "pi-extension", "openai", "fast", "service-tier"],
  "license": "MIT",
  "pi": {
    "extensions": ["./extensions/fast.ts"]
  },
  "scripts": {
    "test": "node --test --experimental-strip-types tests/*.test.ts"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*"
  },
  "files": [
    "extensions",
    "package.json",
    "README.md"
  ]
}
